name: Build & Release ANSAC

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  release:
    types: [ created ]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v4
      with:
        gradle-version: '8.5'

    - name: Build with Gradle
      run: |
        # Clear potentially corrupted FoliaLib cache
        rm -rf $HOME/.gradle/caches/modules-2/files-2.1/com.tcoded/
        gradle clean build --no-daemon

    - name: Run tests
      run: gradle test --no-daemon
      continue-on-error: true

    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: ANSAC-Build
        path: build/libs/ANSAC-AntiCheat-*.jar

  auto-release:
    needs: build
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v4
      with:
        gradle-version: '8.5'

    - name: Build release JAR
      run: |
        rm -rf $HOME/.gradle/caches/modules-2/files-2.1/com.tcoded/
        gradle clean fatJar --no-daemon

    - name: Verify JAR
      run: |
        echo "JAR size:"
        ls -la build/libs/ANSAC-AntiCheat-*.jar
        echo "JAR contents (first 40 entries):"
        jar tf build/libs/ANSAC-AntiCheat-*.jar | head -40

    - name: Get version
      id: version
      run: |
        SHORT_SHA=$(git rev-parse --short HEAD)
        BUILD_DATE=$(date +'%Y%m%d-%H%M')
        echo "tag=build-${BUILD_DATE}-${SHORT_SHA}" >> $GITHUB_OUTPUT
        echo "name=Build ${BUILD_DATE} (${SHORT_SHA})" >> $GITHUB_OUTPUT

    - name: Delete old auto-release
      run: |
        gh release list --json tagName,isPrerelease -q '.[] | select(.tagName | startswith("build-")) | .tagName' | while read tag; do
          echo "Deleting old release: $tag"
          gh release delete "$tag" --yes --cleanup-tag || true
        done
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: Create auto-release
      uses: softprops/action-gh-release@v2
      with:
        tag_name: ${{ steps.version.outputs.tag }}
        name: ${{ steps.version.outputs.name }}
        body: |
          ## ANSAC Auto-Build

          **Commit:** `${{ github.sha }}`
          **Branch:** `${{ github.ref_name }}`
          **Build Date:** $(date +'%Y-%m-%d %H:%M:%S UTC')

          > This is an automated pre-release build. Download `ANSAC-AntiCheat.jar` below.
        files: build/libs/ANSAC-AntiCheat-*.jar
        prerelease: true
        make_latest: false
        token: ${{ secrets.GITHUB_TOKEN }}

  official-release:
    needs: build
    runs-on: ubuntu-latest
    if: github.event_name == 'release'

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v4
      with:
        gradle-version: '8.5'

    - name: Build release JAR
      run: gradle clean fatJar --no-daemon --no-build-cache

    - name: Upload Release Asset
      uses: softprops/action-gh-release@v2
      with:
        files: build/libs/ANSAC-AntiCheat-*.jar
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
